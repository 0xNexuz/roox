import RooxApp from '@/components/roox-app';
import { pageMetadata } from '@/lib/pages';

export const metadata = pageMetadata('providers');
export default function Providers() { return <RooxApp page="providers" />; }
