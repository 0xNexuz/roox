# Server-only operator pilot. Never put these keys in VITE_* or NEXT_PUBLIC_*.
# The endpoint remains closed until every value is configured deliberately.
ROOX_INFERENCE_ENABLED=false
ROOX_GATEWAY_KEY=
ROOX_WORKER_ORIGIN=
ROOX_WORKER_KEY=
ROOX_MODEL=
